# Copy this file to .env.local and fill in your Supabase project credentials.
# Until these are set, the app runs entirely on local mock data (see src/data/mockData.ts).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
